[project]
description = "The dependencies required to run the integration tests"
name = "serde-arrow"
version = "0.0.0"
requires-python = ">=3.14"

dependencies = [
    "pyarrow~=24.0",
    "matplotlib~=3.11",
    "numpy~=2.4",
    "ruff~=0.15.7",
    "polars~=1.41",
]

[tool.uv]
package = false
