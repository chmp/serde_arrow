[project]
description = "The dependencies required to run the integration tests"
name = "serde-arrow"
version = "0.0.0"
requires-python = ">=3.14"

dependencies = [
    "pyarrow~=25.0",
    "matplotlib~=3.11",
    "numpy~=2.5",
    "ruff~=0.15.22",
    "polars~=1.42",
]

[tool.uv]
package = false
