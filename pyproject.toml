[project]
description = "The dependencies required to run the integration tests"
name = "serde-arrow"
version = "0.0.0"
requires-python = ">=3.14"

dependencies = [
    "pyarrow~=25.0",
    "matplotlib~=3.11",
    "numpy~=2.5",
    "ruff~=0.16.8",
    "polars~=1.44",
]

[tool.uv]
package = false
